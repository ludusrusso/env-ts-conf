{
  "name": "env-ts-conf",
  "version": "1.0.0",
  "description": "A simple typed library to load configuration from env files",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "env",
    "configuration",
    "types"
  ],
  "author": "ludusrusso",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.0.26"
  }
}
